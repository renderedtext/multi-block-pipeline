version: v1.0
name: Run on master only
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: First block
    task:
      jobs:
        - commands:
            - if [ "$SEMAPHORE_GIT_BRANCH" != "master" ]; then false; fi
  - name: B
    task:
      jobs:
        - commands:
            -  echo executing on master...

